import { webCloudbeesProvider } from './web-cloudbees-provider';

describe('webCloudbeesProvider', () => {
  it('should work', () => {
    expect(webCloudbeesProvider()).toEqual('web-cloudbees-provider');
  });
});
